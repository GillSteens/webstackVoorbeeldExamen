@model IEnumerable<VoorbeeldExamen.Models.Project>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HuidigBudget)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HuidigBudget)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectNaam }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectNaam }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectNaam })
        </td>
    </tr>
}

</table>
